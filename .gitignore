# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/
docs/plans

# One-time migration scripts, run by hand against live deployments
deploy/migrate-off-public.sql

# Benchmark output (scripts/bench)
scripts/bench/results/

# Per-user Claude Code state (personal settings + runtime locks).
# Shared `.claude/settings.json` and similar can still be committed.
.claude/settings.local.json
.claude/scheduled_tasks.lock
# Scratch page for trying nav-status treatments (see components/nav-status.tsx)
apps/web/src/routes/$orgSlug/toasts.tsx
