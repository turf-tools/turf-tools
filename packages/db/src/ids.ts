// Stable identifiers for seeded data. Defined here (not just in `mock.ts`)
// so any consumer that needs to reference the seeded admin or org — auth
// bypass, tests — can import the canonical value without duplicating the
// literal.

export const SEEDED_ADMIN_USER_ID = "00000000-0000-4000-8000-000000000001";
export const SEEDED_ORG_ID = "00000000-0000-4000-8000-000000000001";
