// The App Review escape hatch: if Apple objects to requiring personal info
// (guideline 5.1.1), flip this to false and resubmit — turf opens then
// proceed even when the canvasser sheet is dismissed without saving.
export const REQUIRE_ATTRIBUTION = true;
